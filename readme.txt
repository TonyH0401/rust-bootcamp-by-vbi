- this is the master branch
- After you clone this from the HTTPS on github
- Use git checkout <branch-name> to access other branches
- For master branch, it is "git push orign master" as per-usual
- For other branches, it is "git push origin head"

- Master branch is locked in the setting, ignore the remote warning